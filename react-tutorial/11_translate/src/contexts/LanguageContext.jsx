import React from "react";

// creating and passing a default value to the context
// it is not mandatory to pass a default value
// this automatically creates both the Provider and Consumer components
export default React.createContext("english");
